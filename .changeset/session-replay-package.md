---
'@pydantic/logfire-session-replay': minor
---

Add a standalone browser session replay package with rrweb recording, gzip chunk uploads, proxy-first replay transport, direct token escape hatch, privacy defaults, sampling, and trace/session correlation hooks.
