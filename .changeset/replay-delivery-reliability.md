---
'@pydantic/logfire-session-replay': patch
---

Make replay delivery more reliable with bounded concurrent lifecycle uploads,
CSP-safe compression fallback, `Retry-After` handling, and UTF-8 byte accounting.
